import{_ as o,c as r,o as t,ag as a}from"./chunks/framework.BHpayLOB.js";const y=JSON.parse('{"title":"Directory Structure","description":"","frontmatter":{},"headers":[],"relativePath":"structure.md","filePath":"structure.md"}'),i={name:"structure.md"};function c(d,e,s,n,l,h){return t(),r("div",null,e[0]||(e[0]=[a('<h1 id="directory-structure" tabindex="-1">Directory Structure <a class="header-anchor" href="#directory-structure" aria-label="Permalink to &quot;Directory Structure&quot;">​</a></h1><ul><li><a href="#introduction">Introduction</a></li><li><a href="#the-root-directory">The Root Directory</a><ul><li><a href="#the-root-app-directory">The <code>app</code> Directory</a></li><li><a href="#the-bootstrap-directory">The <code>bootstrap</code> Directory</a></li><li><a href="#the-config-directory">The <code>config</code> Directory</a></li><li><a href="#the-database-directory">The <code>database</code> Directory</a></li><li><a href="#the-public-directory">The <code>public</code> Directory</a></li><li><a href="#the-listens-directory">The <code>listrns</code> Directory</a></li><li><a href="#the-storage-directory">The <code>storage</code> Directory</a></li><li><a href="#the-vendor-directory">The <code>vendor</code> Directory</a></li></ul></li><li><a href="#the-app-directory">The App Directory</a><ul><li><a href="#the-console-directory">The <code>Console</code> Directory</a></li><li><a href="#the-controllers-directory">The <code>Controller</code> Directory</a></li><li><a href="#the-conversations-directory">The <code>Conversation</code> Directory</a></li><li><a href="#the-events-directory">The <code>Events</code> Directory</a></li><li><a href="#the-exceptions-directory">The <code>Exceptions</code> Directory</a></li><li><a href="#the-jobs-directory">The <code>Jobs</code> Directory</a></li><li><a href="#the-listeners-directory">The <code>Listeners</code> Directory</a></li><li><a href="#the-models-directory">The <code>Models</code> Directory</a></li><li><a href="#the-middlewares-directory">The <code>Middlewares</code> Directory</a></li><li><a href="#the-policies-directory">The <code>Policies</code> Directory</a></li><li><a href="#the-providers-directory">The <code>Providers</code> Directory</a></li><li><a href="#the-rules-directory">The <code>Rules</code> Directory</a></li><li><a href="#the-templates-directory">The <code>Templates</code> Directory</a></li></ul></li></ul><p><a name="introduction"></a></p><h2 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h2><p>The default LaraGram application structure is intended to provide a great starting point for both large and small applications. But you are free to organize your application however you like. LaraGram imposes almost no restrictions on where any given class is located - as long as Composer can autoload the class.</p><p><a name="the-root-directory"></a></p><h2 id="the-root-directory" tabindex="-1">The Root Directory <a class="header-anchor" href="#the-root-directory" aria-label="Permalink to &quot;The Root Directory&quot;">​</a></h2><p><a name="the-root-app-directory"></a></p><h3 id="the-app-directory" tabindex="-1">The App Directory <a class="header-anchor" href="#the-app-directory" aria-label="Permalink to &quot;The App Directory&quot;">​</a></h3><p>The <code>app</code> directory contains the core code of your application. We&#39;ll explore this directory in more detail soon; however, almost all of the classes in your application will be in this directory.</p><p><a name="the-bootstrap-directory"></a></p><h3 id="the-bootstrap-directory" tabindex="-1">The Bootstrap Directory <a class="header-anchor" href="#the-bootstrap-directory" aria-label="Permalink to &quot;The Bootstrap Directory&quot;">​</a></h3><p>The <code>bootstrap</code> directory contains the <code>app.php</code> file which bootstraps the framework. This directory also houses a <code>cache</code> directory which contains framework generated files for performance optimization such as the listen and services cache files.</p><p><a name="the-config-directory"></a></p><h3 id="the-config-directory" tabindex="-1">The Config Directory <a class="header-anchor" href="#the-config-directory" aria-label="Permalink to &quot;The Config Directory&quot;">​</a></h3><p>The <code>config</code> directory, as the name implies, contains all of your application&#39;s configuration files. It&#39;s a great idea to read through all of these files and familiarize yourself with all of the options available to you.</p><p><a name="the-database-directory"></a></p><h3 id="the-database-directory" tabindex="-1">The Database Directory <a class="header-anchor" href="#the-database-directory" aria-label="Permalink to &quot;The Database Directory&quot;">​</a></h3><p>The <code>database</code> directory contains your database migrations, model factories, and seeds. If you wish, you may also use this directory to hold an SQLite database.</p><p><a name="the-public-directory"></a></p><h3 id="the-public-directory" tabindex="-1">The Public Directory <a class="header-anchor" href="#the-public-directory" aria-label="Permalink to &quot;The Public Directory&quot;">​</a></h3><p>The <code>public</code> directory contains the <code>index.php</code> file, which is the entry point for all requests entering your application and configures autoloading. This directory also houses your assets such as images.</p><p><a name="the-listens-directory"></a></p><h3 id="the-listens-directory" tabindex="-1">The Listens Directory <a class="header-anchor" href="#the-listens-directory" aria-label="Permalink to &quot;The Listens Directory&quot;">​</a></h3><p>The <code>listens</code> directory contains all of the listen definitions for your application. By default, two listen files are included with LaraGram: <code>bot.php</code> and <code>console.php</code>.</p><p>The <code>bot.php</code> file contains listens that LaraGram places in the <code>bot</code> middleware group.</p><p>The <code>console.php</code> file is where you may define all of your closure-based console commands. Each closure is bound to a command instance allowing a simple approach to interacting with each command&#39;s IO methods. Even though this file does not define listens, it defines console based entry points (listens) into your application. You may also <a href="/scheduling.html">schedule</a> tasks in the <code>console.php</code> file.</p><p><a name="the-storage-directory"></a></p><h3 id="the-storage-directory" tabindex="-1">The Storage Directory <a class="header-anchor" href="#the-storage-directory" aria-label="Permalink to &quot;The Storage Directory&quot;">​</a></h3><p>The <code>storage</code> directory contains your logs, compiled Temple8 templates, file caches, and other files generated by the framework. This directory is segregated into <code>app</code>, <code>framework</code>, and <code>logs</code> directories. The <code>app</code> directory may be used to store any files generated by your application. The <code>framework</code> directory is used to store framework generated files and caches. Finally, the <code>logs</code> directory contains your application&#39;s log files.</p><p>The <code>storage/app/public</code> directory may be used to store user-generated files, such as profile avatars, that should be publicly accessible. You should create a symbolic link at <code>public/storage</code> which points to this directory. You may create the link using the <code>php laragram storage:link</code> Commander command.</p><p><a name="the-vendor-directory"></a></p><h3 id="the-vendor-directory" tabindex="-1">The Vendor Directory <a class="header-anchor" href="#the-vendor-directory" aria-label="Permalink to &quot;The Vendor Directory&quot;">​</a></h3><p>The <code>vendor</code> directory contains your <a href="https://getcomposer.org" target="_blank" rel="noreferrer">Composer</a> dependencies.</p><p><a name="the-app-directory"></a></p><h2 id="the-app-directory-1" tabindex="-1">The App Directory <a class="header-anchor" href="#the-app-directory-1" aria-label="Permalink to &quot;The App Directory&quot;">​</a></h2><p>The majority of your application is housed in the <code>app</code> directory. By default, this directory is namespaced under <code>App</code> and is autoloaded by Composer using the <a href="https://www.php-fig.org/psr/psr-4/" target="_blank" rel="noreferrer">PSR-4 autoloading standard</a>.</p><p>By default, the <code>app</code> directory contains the <code>Controllers</code>, <code>Models</code>, and <code>Providers</code> directories. However, over time, a variety of other directories will be generated inside the app directory as you use the make Commander commands to generate classes. For example, the <code>app/Console</code> directory will not exist until you execute the <code>make:command</code> Commander command to generate a command class.</p><p>Both the <code>Console</code> and <code>Controllers</code> directories are further explained in their respective sections below, but think of the <code>Console</code> and <code>Controllers</code> directories as providing an API into the core of your application. The Bot updates and CLI are both mechanisms to interact with your application, but do not actually contain application logic. In other words, they are two ways of issuing commands to your application. The <code>Console</code> directory contains all of your Commander commands, while the <code>Controllers</code> directory contains your controllers.</p><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>Many of the classes in the <code>app</code> directory can be generated by Commander via commands. To review the available commands, run the <code>php laragram list make</code> command in your terminal.</p></div><p><a name="the-console-directory"></a></p><h3 id="the-console-directory" tabindex="-1">The Console Directory <a class="header-anchor" href="#the-console-directory" aria-label="Permalink to &quot;The Console Directory&quot;">​</a></h3><p>The <code>Console</code> directory contains all of the custom Commander commands for your application. These commands may be generated using the <code>make:command</code> command.</p><p><a name="the-controllers-directory"></a></p><h3 id="the-controllers-directory" tabindex="-1">The Controllers Directory <a class="header-anchor" href="#the-controllers-directory" aria-label="Permalink to &quot;The Controllers Directory&quot;">​</a></h3><p>The <code>Controllers</code> directory contains your controllers. Almost all of the logic to handle requests entering your application will be placed in this directory.</p><p><a name="the-conversations-directory"></a></p><h3 id="the-conversations-directory" tabindex="-1">The Conversations Directory <a class="header-anchor" href="#the-conversations-directory" aria-label="Permalink to &quot;The Conversations Directory&quot;">​</a></h3><p>The <code>Conversations</code> directory contains all of the conversations classes for your application. These classes are generated using the <code>make:conversation</code> command. This directory does not exist by default, but will be created for you when you create your first conversation. To learn more about conversation, check out the documentation on [conversation].</p><p><a name="the-events-directory"></a></p><h3 id="the-events-directory" tabindex="-1">The Events Directory <a class="header-anchor" href="#the-events-directory" aria-label="Permalink to &quot;The Events Directory&quot;">​</a></h3><p>This directory does not exist by default, but will be created for you by the <code>event:generate</code> and <code>make:event</code> Commander commands. The <code>Events</code> directory houses <a href="/events.html">event classes</a>. Events may be used to alert other parts of your application that a given action has occurred, providing a great deal of flexibility and decoupling.</p><p><a name="the-exceptions-directory"></a></p><h3 id="the-exceptions-directory" tabindex="-1">The Exceptions Directory <a class="header-anchor" href="#the-exceptions-directory" aria-label="Permalink to &quot;The Exceptions Directory&quot;">​</a></h3><p>The <code>Exceptions</code> directory contains all of the custom exceptions for your application. These exceptions may be generated using the <code>make:exception</code> command.</p><p><a name="the-jobs-directory"></a></p><h3 id="the-jobs-directory" tabindex="-1">The Jobs Directory <a class="header-anchor" href="#the-jobs-directory" aria-label="Permalink to &quot;The Jobs Directory&quot;">​</a></h3><p>This directory does not exist by default, but will be created for you if you execute the <code>make:job</code> Commander command. The <code>Jobs</code> directory houses the <a href="/queues.html">queueable jobs</a> for your application. Jobs may be queued by your application or run synchronously within the current request lifecycle. Jobs that run synchronously during the current request are sometimes referred to as &quot;commands&quot; since they are an implementation of the <a href="https://en.wikipedia.org/wiki/Command_pattern" target="_blank" rel="noreferrer">command pattern</a>.</p><p><a name="the-listeners-directory"></a></p><h3 id="the-listeners-directory" tabindex="-1">The Listeners Directory <a class="header-anchor" href="#the-listeners-directory" aria-label="Permalink to &quot;The Listeners Directory&quot;">​</a></h3><p>This directory does not exist by default, but will be created for you if you execute the <code>event:generate</code> or <code>make:listener</code> Commander commands. The <code>Listeners</code> directory contains the classes that handle your <a href="/events.html">events</a>. Event listeners receive an event instance and perform logic in response to the event being fired. For example, a <code>UserRegistered</code> event might be handled by a <code>SendWelcomeEmail</code> listener.</p><p><a name="the-models-directory"></a></p><h3 id="the-models-directory" tabindex="-1">The Models Directory <a class="header-anchor" href="#the-models-directory" aria-label="Permalink to &quot;The Models Directory&quot;">​</a></h3><p>The <code>Models</code> directory contains all of your <a href="/eloquent.html">Eloquent model classes</a>. The Eloquent ORM included with LaraGram provides a beautiful, simple ActiveRecord implementation for working with your database. Each database table has a corresponding &quot;Model&quot; which is used to interact with that table. Models allow you to query for data in your tables, as well as insert new records into the table.</p><p><a name="the-middlewares-directory"></a></p><h3 id="the-middlewares-directory" tabindex="-1">The Middlewares Directory <a class="header-anchor" href="#the-middlewares-directory" aria-label="Permalink to &quot;The Middlewares Directory&quot;">​</a></h3><p>The <code>Middlewares</code> directory contains all of your Middlewares. The middlewares handle requests entering your application.</p><p><a name="the-policies-directory"></a></p><h3 id="the-policies-directory" tabindex="-1">The Policies Directory <a class="header-anchor" href="#the-policies-directory" aria-label="Permalink to &quot;The Policies Directory&quot;">​</a></h3><p>This directory does not exist by default, but will be created for you if you execute the <code>make:policy</code> Commander command. The <code>Policies</code> directory contains the <a href="/authorization.html">authorization policy classes</a> for your application. Policies are used to determine if a user can perform a given action against a resource.</p><p><a name="the-providers-directory"></a></p><h3 id="the-providers-directory" tabindex="-1">The Providers Directory <a class="header-anchor" href="#the-providers-directory" aria-label="Permalink to &quot;The Providers Directory&quot;">​</a></h3><p>The <code>Providers</code> directory contains all of the <a href="/providers.html">service providers</a> for your application. Service providers bootstrap your application by binding services in the service container, registering events, or performing any other tasks to prepare your application for incoming requests.</p><p>In a fresh LaraGram application, this directory will already contain the <code>AppServiceProvider</code>. You are free to add your own providers to this directory as needed.</p><p><a name="the-rules-directory"></a></p><h3 id="the-rules-directory" tabindex="-1">The Rules Directory <a class="header-anchor" href="#the-rules-directory" aria-label="Permalink to &quot;The Rules Directory&quot;">​</a></h3><p>This directory does not exist by default, but will be created for you if you execute the <code>make:rule</code> Commander command. The <code>Rules</code> directory contains the custom validation rule objects for your application. Rules are used to encapsulate complicated validation logic in a simple object. For more information, check out the <a href="/validation.html">validation documentation</a>.</p><p><a name="the-templates-directory"></a></p><h3 id="the-templates-directory" tabindex="-1">The Templates Directory <a class="header-anchor" href="#the-templates-directory" aria-label="Permalink to &quot;The Templates Directory&quot;">​</a></h3><p>This directory does not exist by default, but will be created for you if you execute the <code>make:template</code> Commander command. The <code>templates</code> directory contains the messages templates for your application. For more information, check out the <a href="/temple8.html">Temple8 Engine</a>.</p>',80)]))}const u=o(i,[["render",c]]);export{y as __pageData,u as default};
